[package]
name = "stwo_verifier_contract"
version = "0.1.0"
edition = "2024_07"

[dependencies]
starknet = "2.18.0"
stwo_cairo_air = { path = "../../vendor/stwo_cairo_verifier/crates/cairo_air" }

[features]
default = ["poseidon252_verifier"]
poseidon252_verifier = ["stwo_cairo_air/poseidon252_verifier"]

[[target.starknet-contract]]
sierra = true
# Deployability probe: enforce the allowlist used for declared classes.
allowed-libfuncs-list.name = "audited"

